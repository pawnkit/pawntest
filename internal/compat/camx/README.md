# Canonical AMX sources

These files are copied unchanged from
`openmultiplayer/compiler` commit `bca794a1bc17c6bcb60c1fd0360b4daaff63d50a`:

- `source/amx/amx.c`
- `source/amx/amx.h`
- `source/amx/osdefs.h`
- `source/linux/sclinux.h`
- `source/linux/getch.h`

They are compiled only for compatibility tests using the `amx_c` build tag.
The original zlib-style license notice is retained at the top of `amx.c` and
`amx.h`.
