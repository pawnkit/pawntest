native __pawntest_fail(const message[], const file[], line);
native __pawntest_skip(const reason[], const file[], line);
native __pawntest_use_fixture(const setup_callback[], const teardown_callback[], const file[], line);
