native __pawntest_schedule(delay_ms, const callback_name[]);
native __pawntest_advance_time(milliseconds);
native __pawntest_run_pending();
native __pawntest_now();
native __pt_player_create(const name[]);
native __pt_player_connected(playerid, bool:connected, const file[], line);
native __pt_player_money(playerid, expected, const file[], line);
native __pt_player_message(playerid, const expected[], bool:contains, const file[], line);
native __pt_player_pos_near(playerid, Float:x, Float:y, Float:z, Float:tolerance, const file[], line);
native __pt_vehicle_create(modelid, Float:x, Float:y, Float:z);
native __pt_vehicle_valid(vehicleid, const file[], line);
native __pt_vehicle_model(vehicleid, modelid, const file[], line);
native __pt_vehicle_health(vehicleid, Float:health, const file[], line);
native __pt_vehicle_pos_near(vehicleid, Float:x, Float:y, Float:z, Float:tolerance, const file[], line);
native __pt_object_create(modelid, Float:x, Float:y, Float:z);
native __pt_object_valid(objectid, const file[], line);
native __pt_object_model(objectid, modelid, const file[], line);
native __pt_object_pos_near(objectid, Float:x, Float:y, Float:z, Float:tolerance, const file[], line);
native __pt_player_object_create(playerid, modelid, Float:x, Float:y, Float:z);
native __pt_player_object_valid(playerid, objectid, const file[], line);
native __pt_actor_create(skin, Float:x, Float:y, Float:z, Float:angle);
native __pt_actor_valid(actorid, const file[], line);
native __pt_actor_skin(actorid, skin, const file[], line);
native __pt_actor_health(actorid, Float:health, const file[], line);
native __pt_actor_pos_near(actorid, Float:x, Float:y, Float:z, Float:tolerance, const file[], line);
native __pt_pickup_create(model, type, Float:x, Float:y, Float:z, virtualWorld);
native __pt_pickup_valid(pickupid, const file[], line);
native __pt_pickup_model(pickupid, model, const file[], line);
native __pt_pickup_pos_near(pickupid, Float:x, Float:y, Float:z, Float:tolerance, const file[], line);
native __pt_player_pickup_create(playerid, model, type, Float:x, Float:y, Float:z, virtualWorld);
native __pt_player_pickup_valid(playerid, pickupid, const file[], line);
native __pt_checkpoint_active(playerid, bool:active, const file[], line);
native __pt_checkpoint_inside(playerid, bool:inside, const file[], line);
native __pt_race_checkpoint_active(playerid, bool:active, const file[], line);
native __pt_race_checkpoint_inside(playerid, bool:inside, const file[], line);
native __pt_text_label_create(const text[], colour, Float:x, Float:y, Float:z, Float:drawDistance, virtualWorld);
native __pt_text_label_valid(textid, const file[], line);
native __pt_text_label_text(textid, const expected[], const file[], line);
native __pt_player_text_label_create(playerid, const text[], colour, Float:x, Float:y, Float:z, Float:drawDistance);
native __pt_player_text_label_valid(playerid, textid, const file[], line);
native __pt_player_text_label_text(playerid, textid, const expected[], const file[], line);
native __pt_textdraw_create(Float:x, Float:y, const text[]);
native __pt_textdraw_valid(textid, const file[], line);
native __pt_textdraw_text(textid, const expected[], const file[], line);
native __pt_textdraw_visible(playerid, textid, bool:visible, const file[], line);
native __pt_player_textdraw_create(playerid, Float:x, Float:y, const text[]);
native __pt_player_textdraw_valid(playerid, textid, const file[], line);
native __pt_player_textdraw_text(playerid, textid, const expected[], const file[], line);
native __pt_player_textdraw_visible(playerid, textid, bool:visible, const file[], line);
native __pt_gangzone_create(Float:minX, Float:minY, Float:maxX, Float:maxY);
native __pt_gangzone_valid(zoneid, const file[], line);
native __pt_gangzone_visible(playerid, zoneid, bool:visible, const file[], line);
native __pt_gangzone_inside(playerid, zoneid, bool:inside, const file[], line);
native __pt_player_gangzone_create(playerid, Float:minX, Float:minY, Float:maxX, Float:maxY);
native __pt_player_gangzone_valid(playerid, zoneid, const file[], line);
native __pt_player_gangzone_visible(playerid, zoneid, bool:visible, const file[], line);
native __pt_dialog_respond(playerid, bool:response, listitem, const inputtext[]);
native __pt_dialog_visible(playerid, dialogid, bool:visible, const file[], line);
native __pt_dialog_title(playerid, const expected[], const file[], line);
native __pt_dialog_body(playerid, const expected[], const file[], line);
native __pt_menu_select(playerid, row);
native __pt_menu_exit(playerid);
native __pt_menu_valid(menuid, const file[], line);
native __pt_menu_visible(playerid, menuid, bool:visible, const file[], line);
native __pt_menu_items(menuid, column, expected, const file[], line);
native __pt_class_select(playerid, classid);
native __pt_class_count(expected, const file[], line);
native __pt_player_class(playerid, classid, const file[], line);
native __pt_player_selecting_class(playerid, bool:selecting, const file[], line);
native __pt_svar_int(const name[], expected, const file[], line);
native __pt_svar_float(const name[], Float:expected, Float:tolerance, const file[], line);
native __pt_svar_string(const name[], const expected[], const file[], line);
native __pt_pvar_int(playerid, const name[], expected, const file[], line);
native __pt_pvar_float(playerid, const name[], Float:expected, Float:tolerance, const file[], line);
native __pt_pvar_string(playerid, const name[], const expected[], const file[], line);
native __pt_server_weather(expected, const file[], line);
native __pt_server_time(expected, const file[], line);
native __pt_server_gravity(Float:expected, Float:tolerance, const file[], line);
native __pt_server_mode_text(const expected[], const file[], line);
native __pt_server_rule(const name[], const expected[], const file[], line);
native __pt_npc_create(const name[]);
native __pt_npc_valid(npcid, const file[], line);
native __pt_npc_spawned(npcid, bool:spawned, const file[], line);
native __pt_npc_health(npcid, Float:expected, Float:tolerance, const file[], line);
native __pt_npc_pos_near(npcid, Float:x, Float:y, Float:z, Float:tolerance, const file[], line);
native __pt_npc_weapon(npcid, expected, const file[], line);
native __pt_npc_aiming(npcid, bool:aiming, const file[], line);
native __pt_npc_animation(npcid, const library[], const name[], const file[], line);
native __pt_npc_playback(npcid, bool:playing, const file[], line);
native __pt_npc_path_count(expected, const file[], line);
native __pt_npc_record_count(expected, const file[], line);
